# 更新日志

## [Unreleased]

## [1.0.10] - 2026-08-15

### 新增
- 服务与算法设置：FSRS 权重、最大间隔、图片遮罩模式、关闭通知、IR 源记忆卡首次学习时间可在同一面板调整
- AI 快捷制卡（自动）：由模型自行分配卡型；默认快捷键 `Alt+C`
- 快捷制卡：单次生成上限、逐张勾选保留、对照源块已有卡片去重
- AI 制卡优先使用当前选区作为源文（单 fragment / 同块跨样式 / 跨块）
- 后台 AI 任务可在源块行尾取消；同源块多任务一次取消全部
- 网页导入：AI 总结可跳过；长文按开头/中段/结尾采样
- 渐进阅读：可配置紧凑原生选区工具栏；内容读取部分失败时给出非阻断提示
- 删除卡片闭环：解包/降级内容并清除复习进度

### 变更
- 选择题不再使用独立 `#choice` 标签，统一以 `type=choice` 识别
- 选择题未揭晓时隐藏选项标签，并按卡片冻结洗牌顺序
- 方向卡撤销会对称回滚身份；填空/方向卡复习面禁止行内编辑
- 卡片收集失败不再伪装成空结果；列表卡写入类型后立即刷新缓存
- 章末小测：源文块不完整时终止生成并允许对可恢复错误重试
- 块解释的举例/反驳请求状态相互隔离；摘录与主题可作为 AI 源文
- 快捷制卡「保留」失败时任务仍可重试，不会静默丢失

### 已知限制
- EPUB ZIP 中央目录声明体积尚未独立预检，仍依赖实际解压预算限制。
- EPUB、网页导入和 AI 保存的写入阶段尚未统一为可中断事务。

## [1.0.9] - 2026-08-08

### 新增
- 图片遮罩（Image Occlusion）：编辑器绘制矩形遮罩、复习模式、cloze 编号迁移与压缩
- Azure TTS：选区生成语音、撤销、多语言 voice 与预览；卡列表批量 TTS
- 卡片浏览器：筛选/查询、批量暂停/激活/重置/换卡组
- 暂停卡片视图（SuspendedCardsView）：列出并恢复暂停卡
- 复习服务设置面板：与算法设置分离；Pass-Fail 按钮与显示下次复习时间等 UI 偏好
- AI 源选区：跨样式 / 跨块选区与有界子树展开；Quick Interact / 快捷制卡 / 制卡流共用；前序连续区间（兄弟 / 父子 / 跨分支）
- 章末小测生成偏好（题量 / 语言 / 自定义指令 / 专用模型）与学习闭环完善

### 变更
- IR 富文本摘录解析支持前序链与结构截断预算，跨块 AI 源挂靠更稳妥
- 复习与渐进阅读工作流扩展（卡组/会话侧配套）

### 已知限制
- EPUB ZIP 中央目录声明体积尚未独立预检，仍依赖实际解压预算限制。
- EPUB、网页导入和 AI 保存的写入阶段尚未统一为可中断事务。

## [1.0.8] - 2026-08-01

### 新增
- 渐进阅读章末小测：制卡校验、自动打开制卡面板、题目原文一键定位、动态进度条与更清晰的头部布局
- 摘录教练（Extract Coach）：结合上下文给出摘录处理建议
- AI 服务设置对话框改为分段标签页，导航与组织更清晰
- Gemini `google_search` 序列化为嵌套 grounding 工具；web search 按模型自动路由

### 变更
- IR 间隔分散改为仅影响 due：`ir.intervalDays` 保持既定节奏，优先级感知偏移只作用于到期日，批量 topic / 摘录回流分散到多天
- 顺序阅读进度中的摘录处理增强
- 修复复习显示答案后题面 inline 渲染丢失；章末小测「原文」优先在左侧 IR 阅读面板内定位

### 已知限制
- EPUB ZIP 中央目录声明体积尚未独立预检，仍依赖实际解压预算限制。
- EPUB、网页导入和 AI 保存的写入阶段尚未统一为可中断事务。

## [1.0.7] - 2026-07-29

### 新增
- IR 新建条目初始 due 策略：按优先级分散到期日，避免同日扎堆
- 插件加载时 `ensureCardTagProperties`：新用户无需先建卡即可使用 Book IR / Topic
- 挖空卡 / 方向卡 / IR 转卡等创建路径接入初始 due 策略（不覆盖已有卡排期）

### 变更
- `#card` 标签属性初始化：并发单飞、失败可见通知、初始化失败时插件仍可运行
- 渐进阅读会话校验 `insertBlock` 返回值；列表卡写入 `srs.isCard` 后立即 invalidate cache
- `scanCardsFromTags` 标签查询与 fallback 错误上报加固

### 已知限制
- EPUB ZIP 中央目录声明体积尚未独立预检，仍依赖实际解压预算限制。
- EPUB、网页导入和 AI 保存的写入阶段尚未统一为可中断事务。

## [1.0.6] - 2026-07-28

### 新增
- AI 制卡：详情级别 / 语言 / 自定义指令、重试与并发闸门、用量统计与请求日志、选择题混出、输出 token 预算可配置
- AI 快捷制卡命令与内联 pending 预览；同批卡片聚类，AI 新卡默认不排期
- 选择题创建与撤销
- Flash Home：阅读库入口、StatChip + TodayProgressRing 学习摘要；可从主页直达渐进阅读工作区
- UI 设计基线：Apple HIG token，复习 / IR / Home / AI 对话框对齐
- 渐进阅读：统一今日学习卡片投递、混合复习卡预检与聚焦 UI
- 渐进阅读：下一篇单步撤销、读完确认闸门；逾期老化与探索采样、遗留积压自动推后（优先级保护）、markAsRead 真实耗时补偿
- FSRS interval fuzz，分散到期日聚集
- EPUB：章节粒度与规划、多 fragment 支持、导入角色与预览增强
- IR 阅读断点视口锚点、内容宽度偏好

### 变更
- 复习：会话视图切换时重置滚动；Again 评分卡同会话重新入队
- 摘录 focus 默认关闭上下文；IR 动作栏文案统一
- 移除未引用的 IRCardList / IRStatistics

### 已知限制
- EPUB ZIP 中央目录声明体积尚未独立预检，仍依赖实际解压预算限制。
- EPUB、网页导入和 AI 保存的写入阶段尚未统一为可中断事务。

## [1.0.5] - 2026-07-25

### 新增
- Quick AI：结果多选确认、标签、复用同一结果块，以及 Prompt「直接写入下方」模式
- 网页导入：可选 AI 摘要生成（导入成功后异步写入，失败不回滚正文）
- 渐进阅读：每日限额与摘录排期反馈、顺序阅读有意义停留时间（SAC）
- 网页预抽取：可视化折叠无关控件，提升 HTML 规范化质量

### 变更
- AI 请求：仅对支持模型附带 `web_search`，并改进后台任务错误通知
- Quick AI 插入与清理：保留未确认候选、复用结果根时安全分离旧任务

### 已知限制
- EPUB ZIP 中央目录声明体积尚未独立预检，仍依赖实际解压预算限制。
- EPUB、网页导入和 AI 保存的写入阶段尚未统一为可中断事务。

## [1.0.4] - 2026-07-22

### 新增
- AI 快捷交互与 Prompt 管理：工具栏快捷交互、Prompt 管理器、Markdown 解析与插入计划
- AI 后台任务与块内预览：加载指示、预览树操作、「保留此块」、离开面板自动 dismiss 预览
- AI / Firecrawl 服务设置面板：原生联网搜索与 reasoning effort 等选项
- 块解释快捷键与 UI 增强（渐进阅读会话）

### 变更
- EPUB / 网页导入从工具栏按钮迁移为 slash 命令入口
- AI 插入文本净化：正确处理 Markdown 链接与数字脚注，避免误链库内低 id 块

### 已知限制
- EPUB ZIP 中央目录声明体积尚未独立预检，仍依赖实际解压预算限制。
- EPUB、网页导入和 AI 保存的写入阶段尚未统一为可中断事务。

## [1.0.2] - 2026-07-20

### 新增
- 渐进阅读块解释（Block Explain）：会话内对当前块生成解释、侧栏内容与追问，并写入规范化子块
- 块解释 UI 与主题变量：`IRBlockExplainInline` / controller，配套 `ir-workspace` 样式

### 变更
- 基础卡答案编辑改为单一 live block，避免双块编辑错位
- 复习内嵌块默认 `initiallyCollapsed: false`，折叠笔记中仍可见正文
- AI 制卡 grounding 校验增强 Markdown 链接识别，并更新 service prompts

### 已知限制
- EPUB ZIP 中央目录声明体积尚未独立预检，仍依赖实际解压预算限制。
- EPUB、网页导入和 AI 保存的写入阶段尚未统一为可中断事务。

## [1.0.1] - 2026-07-20

### 新增
- 闪卡主页单页布局：`HomeSummaryBar` 可点击统计导航、`CardFrame` 状态色条与卡组列表体验增强
- 渐进阅读重要性：用户可见「重要性」文案，建书/导入三档绝对档位与会话相对微调（`Alt+P`）
- 渐进阅读摘录近上下文与章节浏览：定位高亮、上下文 hide-self、browse 返回 focus
- 渐进阅读混合摘录处理：二次挖空（`keep_extract`）并保留 IR 调度
- 顺序阅读完成与归档：章节完成后资料库保留结构节点、正文不删
- 会话动作栏与更多面板：主栏「下一篇 / 摘录|挖空 / 重要性 / 完成 / ⋯」，更多面板 dismiss 逻辑

### 变更
- 闪卡主页移除 Dashboard / 全页统计 Tab，统一为 `FlashHomePage` 单页
- 渐进阅读会话创建路径只读装配队列；资料库「一键溢出推后」与会话启动解耦
- 修复隐藏 IR 工作区布局（hideable 与 inline flex 冲突）

### 已知限制
- EPUB ZIP 中央目录声明体积尚未独立预检，仍依赖实际解压预算限制。
- EPUB、网页导入和 AI 保存的写入阶段尚未统一为可中断事务。

## [1.0.0] - 2026-07-19

### 已知限制
- EPUB ZIP 中央目录声明体积尚未独立预检，仍依赖实际解压预算限制。
- EPUB、网页导入和 AI 保存的写入阶段尚未统一为可中断事务。

### 新增
- 渐进阅读功能：独立阅读面板、优先级调度、面包屑导航与完整操作（标记已读/跳过/生成卡片/删除）
- 渐进阅读状态存储：`ir.priority`、`ir.lastRead`、`ir.readCount`、`ir.due`
- 渐进阅读队列收集与 2:1 混合策略构建
- 渐进阅读管理面板：统计总览、按日期分组排期、侧面板预览、批量调整优先级

### 变更
- 渐进阅读：打开阅读会话/管理面板不再自动修改排期；当到期卡超过每日上限时，提供明确按钮“一键把溢出推后”

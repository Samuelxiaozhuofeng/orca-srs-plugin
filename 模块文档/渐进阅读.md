# 渐进阅读模块

## 功能概述

渐进阅读模块用于管理 Topic（主题）与 Extract（摘录）两类块的阅读节奏，提供独立于 SRS 复习的阅读会话与调度逻辑。当前会话面向**低压体验**：时间盒入口、有限队列、可随时中断的断点恢复、键盘主路径，以及 Topic → Extract → Item 漏斗。

## 自动标签开关

自动标签功能用于在 Topic 下创建子块时，自动补齐 `#card` 标签并设置 `type=extracts`。当前默认关闭，可在命令面板中手动切换：

- 命令：`SRS: 切换渐进阅读自动标签`
- 启用时：新建子块会自动打标签并初始化 Extract
- 关闭时：新建子块不做自动标记

说明：默认值由插件设置项 `enableAutoExtractMark` 控制；也可以在「渐进阅读管理面板」顶部的「队列设置」中直接修改（与插件 Settings 面板同步）。

## 数据模型

渐进阅读状态存储在 Block Properties 中，字段如下：

- `ir.priority`：优先级，0-100（默认 50，数值越大越优先）
- `ir.lastRead`：上次阅读时间（Date 或 null）
- `ir.readCount`：累计阅读次数
- `ir.due`：下次到期时间（Date）
- `ir.intervalDays`：当前间隔（天，可为小数），用于计算下一次 `ir.due`
- `ir.postponeCount`：累计推后次数（手动推后 + 溢出/自动推后）
- `ir.stage`：漏斗阶段（由真实动作推进，见下表）
- `ir.lastAction`：最近一次动作（如 `next` / `postpone` / `autoPostpone` / `priority` / `extract` / `itemize`）
- `ir.position`：Topic 队列位置（数值越小越靠前）
- `ir.resumeBlockId`：继续阅读进度（下次打开卡片时自动跳转到该 blockId）
- `ir.breakpoint`：JSON 阅读断点（preview / selection / updatedAt / version）
- `ir.autoPostponeBatchId`：自动推后批次 ID（撤销时与 `lastAction=autoPostpone` 一起校验；持久化字段）
- `ir.sourceTopicId`：Extract 来源 Topic（创建摘录时写入）
- `ir.sourceBookId` / `ir.sourceBookTitle` / `ir.batchId`：书籍批量初始化来源元数据
- Item 来源追溯（转化成功后写在 Item 上）：`ir.sourceExtractId`、`ir.sourceTopicId`、`ir.sourceTextSnippet`、书籍字段

标签属性补充：

- `#card.priority`：数字优先级（Number）
  - 仅用于 Orca 的标签属性面板展示/筛选
  - 插件会在写入 `ir.priority` 后主动同步该值到 `#card.priority`
  - 调度/选卡逻辑只使用 `ir.priority`，不会读取 `#card.priority`

### 阶段推进（`ir.stage`）

| 当前阶段 | 触发动作 | 下一阶段 |
| --- | --- | --- |
| `topic.preview` | 下一篇 / 创建 Extract | `topic.work` |
| `topic.work` | 继续阅读 | `topic.work` |
| `extract.raw` | 编辑后离开 | `extract.refined` |
| `extract.refined` | 打开制卡工具 | `extract.item_candidate` |
| `extract.item_candidate` | 制卡取消 | `extract.refined` |
| 任意阶段 | 归档 / 完成转化（清理策略） | 清除 IR 状态 |

阶段只用于推荐与漏斗诊断，不阻止用户操作。实现：`src/srs/incremental-reading/irStageTransitions.ts`。

## 调度算法

优先级调度采用单一真相：**只使用 `ir.priority(0-100)`**，并引入“间隔增长”避免材料长期高频出现。

更多“排期 vs 今日推送（队列）”与调试记录见：`模块文档/记忆排期推送.md`。

### 1) 基础间隔（base interval）

- Topic：优先级越高越频繁（约 `14d → 2d`）
- Extract：优先级越高越频繁（约 `7d → 1d`）

实现位于：`src/srs/incrementalReadingScheduler.ts`

### 2) 到期时间（due）

- `due` 由 `baseDate + intervalDays` 计算得到（单位：天）。
- 为减少“同日结块”，系统会在写入排期时对 `intervalDays` 做“分散抖动”
- 实现位于：`src/srs/incrementalReadingDispersal.ts`，写入入口位于：`src/srs/incrementalReadingStorage.ts`
- **Book IR 分散初始化是特殊情况**：各章节的首次 `due` 由书籍阅读计划（`calculateChapterDueDates`）写入，不由 `baseDate + intervalDays` 推算；`intervalDays` 仍取优先级对应的基础间隔（如重要性 50 的 Topic 为 8 天），保留后续阅读节奏，**不会**改成“导入时间到首次到期的天数”。

### 3) 已读/下一篇后的间隔增长

- Topic：`intervalDays *= 1.25`（上限 60 天）
- Extract：`intervalDays *= 1.35`（上限 30 天）

### 4) 调整重要性（优先级）

- **新卡**（`init`/`migrate`）：仍按基础间隔 + 分散初始化（保留新 Extract 排队延迟）。
- **已有间隔的卡片**：按比例修正 `intervalDays`（factor 限制在 `0.6..1.6`），**不会**无条件重置为 1 天基础间隔。
- 会话 UI 使用低/中/高三档（映射 20/50/80），精确数值在管理面板。

## 会话交互语义

主界面高频动作（≤3 + 次级“更多”）：

| 动作 | 含义 |
| --- | --- |
| 下一篇 | 强制保存断点 → 增长间隔并离开当前卡 |
| 摘录 | 从选区创建 Extract（`Alt+X`，可重绑定） |
| 记住 | Extract → Cloze 原子转化（`Alt+Z`） |
| 推后 | 次级菜单/Shift+Enter；写回 due |
| 更多 | 重要性三档、归档等 |

其他：

- 进度显示为 **已完成 / 本次计划**，不再使用「当前索引 / 剩余队列长度」
- 收集失败显示「数据读取失败」，不得伪装成「暂无到期内容」
- 关闭/下一篇/推后/归档前强制 flush 阅读断点；关闭时 flush 失败**不静默关闭**（可重试或强制关闭）
- 打开卡片时恢复：优先 `setSelectionFromCursorData`，否则 `scrollIntoView(resumeBlockId)`
- 恢复以**卡片进入/切换**为生命周期边界：同一 `cardId` + 恢复目标只执行一次，计时器 tick 或通知回调 identity 变化不得重新恢复
- 无文本选区时，滚动停止后用可见块估算 `resumeBlockId`
- 断点保存经单通道队列 + 版本号，旧响应不能覆盖新断点
- 滚动容器与正文容器分离：`.ir-reading__scroll` 承担 `overflow:auto` 与 scroll 监听；`.ir-reading__body` 仅承载 Orca 正文块查询范围
- 选区交互使用 Orca 原生选区工具栏；摘录与 Cloze 通过 `Alt+X` / `Alt+Z`（可重绑定）或底部动作栏触发，不再渲染插件自定义浮动工具栏

### 快捷键

| 默认 | 动作 | 注册方式 |
| --- | --- | --- |
| `Alt+X` | 创建 Extract | Orca `shortcuts.assign`（可重绑定） |
| `Alt+Z` | 创建 Cloze | Orca `shortcuts.assign`（可重绑定） |
| `Enter` | 下一篇 | **仅**会话 React Hook（**不**全局 assign，避免绕过 IME/编辑器保护） |
| `Shift+Enter` | 打开推后 | 仅会话 Hook |
| `Alt+P` | 打开重要性 | 仅会话 Hook |
| `Esc` | 关闭次级菜单 | 仅会话 Hook |

规则：事件须发生在本会话 DOM 内；IME 组合输入、弹窗、INPUT/TEXTAREA 期间停用；编辑器无选区时 Enter 保持换行。会话命令若被用户手动绑定，广播带 `panelId`，只驱动当前活动面板。`Alt+Z` 广播还带 `targetBlockId`（`cursor.anchor.blockId`）：仅当与队列当前卡 ID 一致时 Shell 才 `preventDefault` 并接管——当前为 Topic 且仍在 Topic 正文则提示先创建 Extract；当前为 Extract 则先 flush 断点再走 `convertExtractToItem`。Topic 下新建 Extract 子块后立刻制卡时 `targetBlockId` 为子块 ID，Shell 不拦截，由 `createClozeFromEditorCommand` 识别 Extract 并走同一原子转化服务；普通非 Extract 块仍走常规 Cloze 路径。

## API 参考

### 存储

- `loadIRState` / `saveIRState` / `ensureIRState` / `deleteIRState` / `deleteIRSchedulingState`
- `markAsRead`：下一篇语义，推进 stage，增长间隔
- `updatePriority`：比例修正间隔（非新卡）
- `postpone` / `advanceDueToToday` / `updateReadingBreakpoint`

### 会话服务

- `performNext` / `performPostpone` / `performArchive` / `performPriorityAdjust`
  位于 `src/srs/incremental-reading/irSessionService.ts`

### 原子制卡

- `convertExtractToItem`（`irConversionService.ts`）
  - 转化前快照正文、标签类型、原有 `srs.*`、来源属性与 IR
  - 失败：恢复正文/标签/原有 SRS 与来源属性；只删除本次新增属性；独立半成品 Item 删除
  - 成功且 `complete_extract`：只 `deleteIRSchedulingState`，保留 #card、Cloze SRS 与 `ir.source*` 来源追溯
  - 断点在转化前 flush，避免清理调度字段后被旧保存重新创建
  - `verifyItemCollectable` 同时要求 #card、`type=cloze` 与对应 `srs.cN.*`

### 队列策略与过载

- `selectQueueWithPolicy`：时间预算 + Topic 最低曝光 + 新 Extract 配额；成本用停留校准
- `applyAutoPostpone` / `undoAutoPostponeBatch`：先快照后写入，失败回滚；`ir.autoPostponeBatchId` 持久化

### 收集

- 标签查询失败抛错；`loadIRState` 失败抛错（不用默认状态伪装成功）
- 有限并发；优先读最近 10 分钟内完成全量校验的 IR 索引，过期或失效则全量重建
- Topic、书籍章节、自动 Extract 与手动 Extract 创建后增量入索引；完成或转 Item 后移出索引
- 会话优先走时间盒策略

## 统一工作区 UI（双模式一体）

渐进阅读会话与管理面板已整合为**同一个工作区组件**，顶部以分段控件切换：

- **资料库**：搜索 / 筛选 / 顶部时间带（互斥导航 + 区间计数）/ 三级高密度树形结构（来源 → 章节 → 摘录）/ 批量操作
- **专注阅读**：时间盒启动、沉浸阅读、摘录 / 制卡 / 下一篇 / 推后

模式切换**不销毁**现场：资料库的搜索筛选、时间导航定位、分组折叠展开、多选状态，以及阅读队列与进度均保留。

### 资料库三级树形与时间带架构

为彻底解决传统「按到期日期分组」将同一本书的章节与摘录打散的问题，资料库视图采用 **自底向上聚合与剪枝算法 (`buildIRSourceTree`)** 构建层次化的三级交互架构：

1. **顶部时间导航带 (`IRTimeNavigationBar`)**：提供 `全部 / 逾期 / 今天 / 明天 / 未来 7 天 / 新卡 / 7 天后` 互斥区间导航，并以高密度胶囊展现各区段命中张数，替代原有的单一到期状态过滤器与平面分组条。
2. **第一级 - 来源列表 (`IRLibrarySourceItem`)**：有 `ir.sourceBookId` 的卡片按书籍归拢；非书籍主题以主题自身作为稳定的一般来源，其摘录通过 `ir.sourceTopicId` 归入该来源；无法识别父主题或书籍的摘录进入「未归入来源」。当前数据模型没有独立网页 URL/域名字段，因此不会根据标题猜测或虚构域名分组。
3. **第二级 - 章节列表 (`IRLibraryChapterItem`)**：展开来源即可查看章节分布。章节行常驻展示到期日、阶段、重要性与匹配摘录数；继续展开后才显示命中的主题卡和摘录卡。
4. **第三级 - 摘录列表 (`IRLibraryRow`)**：在章节内缩进展开，直观查看具体的所有摘录卡片；每个摘录行或主题行继续支持多选、查看详情、提前阅读等高密度操作。
5. **自底向上剪枝逻辑**：如果用户搜索关键字或切换至「今天 / 逾期」等时间区段，只有匹配条件的真实卡片参与计数、渲染和批量选择；构建器仍保留其父章节与父来源作为结构上下文，并裁掉空分支。
6. **固定排序**：资料库不提供全局排序控件。来源依次按含逾期、含今天、最近到期时间和来源名称排序；来源内章节按到期时间、标题和 ID 稳定排序。

```text
src/components/incremental-reading/
  IRSessionShell.tsx      # 阅读会话生命周期与主动作
  IRSessionHeader.tsx     # 进度 / 剩余时间
  IRReadingPane.tsx       # 正文优先（无多层边框）
  IRActionBar.tsx         # 下一篇 / 摘录|记住 / 推后 / 更多
  IRPostponeMenu.tsx
  IRSessionSummary.tsx
  IRFunnelDiagnosticsPanel.tsx
  workspace/
    IRWorkspaceShell.tsx      # canonical 工作区外壳
    IRWorkspaceHeader.tsx
    IRModeSwitcher.tsx
    IRLibraryView.tsx         # 资料库视图容器
    IRTimeNavigationBar.tsx   # 顶部时间互斥导航与计数
    IRLibrarySourceTree.tsx   # 三级来源树容器
    IRLibrarySourceItem.tsx   # 第一级：来源节点与操作
    IRLibraryChapterItem.tsx  # 第二级：章节节点与主块
    IRLibraryRow.tsx          # 第三级：具体卡片/摘录行
    IRBulkActionBar.tsx
    IRQueueDrawer.tsx / IRDetailsDrawer.tsx
    IRWorkspaceSettings.tsx   # 设置 + 漏斗诊断 + 危险操作
    irSourceTreeBuilder.ts    # 树形构建器与剪枝算法（含单测）
    irLibraryFilters.ts       # 筛选排序纯函数（含单测）
    useIRWorkspaceLibrary.ts
    useIRWorkspaceSession.ts
src/srs/incremental-reading/
  irWorkspacePanelLaunch.ts   # 原生 block 面板查找与启动
```

样式：`src/styles/ir-workspace.css`（Orca tokens，兼容明暗主题）。构建产物为
`dist/style.css`，插件加载时通过 `orca.themes.injectCSSResource` 注入，卸载时按 role 清理。

正常入口通过 Orca 内建 `view: "block"` 打开 `srs.ir-session` 虚拟块，由
`IncrementalReadingSessionRenderer` 经 `BlockShell` 承载 `IRWorkspaceShell`。
阅读正文仍使用 `orca.components.Block`，以保留原生编辑、选区、断点与制卡能力；
EPUB 图片、表格、列表、引用等内容继续交给 Orca 原生块渲染链处理。

### 兼容入口

| 入口 | 行为 |
| --- | --- |
| `IncrementalReadingSessionRenderer` | `BlockShell` → `IRWorkspaceShell`（阅读入口默认专注阅读） |
| `IncrementalReadingManagerPanel` | 独立 `srs.ir-manager` 块渲染器（历史兼容，非统一工作区主入口） |
| `startIncrementalReadingSession` | 打开或聚焦原生 block 面板中的会话块，切到专注阅读 |
| `openIRManager` | 打开或聚焦同一会话块面板，切到资料库 |
| `IncrementalReadingSessionDemo` | 仍可单独挂 `IRSessionShell`（兼容） |

### 资料库能力

- 文本搜索与排序常驻；类型、来源、阶段、到期状态、重要性按需展开，已启用条件显示数量
- 一行状态摘要，仅保留逾期、今天、新卡与主题/摘录数量
- 表格式高密度行；调度内部字段在详情抽屉
- 单选 / 多选 / 分组全选 / 同批次 / 批量移出
- 今日候选超过每日上限时显示溢出摘要，并保留手动推后入口
- 开始阅读 / 提前阅读；设置与漏斗诊断收纳在抽屉

### 专注阅读能力

- 时间盒 10/20/30 整合在阅读模式内，无独立启动页割裂
- 正文宽度约 720–820px；单一主纵向滚动
- 底部固定动作栏；队列右侧抽屉
- 空队列、读取失败、断点保存失败界面区分

### 混合学习（阅读 + SRS 复习）

可选功能，**默认关闭**。在统一工作区顶栏「设置」抽屉中开启：

- `mixedLearningEnabled`（默认 `false`）
- `mixedLearningReviewRatio`：20% / 30% / 40% 三档分段选择（默认 **30%**）

开启后，每次启动专注阅读时间盒时会构建**固定快照**混合队列：

1. 会话启动时收集 SRS 复习卡，仅纳入**当时已到期、非新卡、未暂停**的条目（Basic / Cloze / Direction 等由 `collectReviewCards()` 展开的稳定身份）。
2. 有符合条件的复习卡时，按所选比例为 SRS 预留时间，再用 `selectQueueWithPolicy()` 生成 Topic/Extract 阅读队列；没有符合条件的复习卡时，完整时间盒都用于阅读。
3. 目标复习数：`floor(R * p / (1 - p))`，其中 `R` 为阅读条数、`p` 为占比；受实际到期卡数量与时间盒剩余预算限制。
4. 复习卡均匀插入阅读条目自然边界之后；**首项必为阅读**；避免连续复习卡；阅读相对顺序不变。
5. 会话中新制 Item、会话中才到期的卡**不进入**当前快照。
6. 若无符合条件复习卡，退化为纯阅读队列，不报错。

复习条目在 `IRSessionShell` 中通过 `IRMixedReviewPane` 复用 `SrsCardDemo` 与 `reviewCardGrading.ts`（FSRS 更新、埋藏、暂停、日志与事件）。评分成功后短暂停留（`IR_MIXED_REVIEW_AUTO_ADVANCE_MS`，默认 800ms）并自动前进，期间可点「继续阅读」；手动与自动前进共用幂等闸门。

独立 SRS 复习入口（`SrsReviewSessionRenderer`）行为不变。

实现：`src/srs/incremental-reading/irMixedQueuePolicy.ts`、`src/components/incremental-reading/IRMixedReviewPane.tsx`

## 性能

- 收集：有限并发 + 惰性 ensure（以 `loadIRState` 为准）
- 可选 IR ID 索引：`src/srs/incremental-reading/irIndex.ts`（localStorage）
- 成本估算可按停留样本校准：`irCostCalibration.ts`
- 跨 fragment / 跨块摘录规划：`irRichExtract.ts`（Extract 创建已接入）

## 相关文件

- `src/srs/incrementalReadingStorage.ts`
- `src/srs/incrementalReadingScheduler.ts`
- `src/srs/incrementalReadingCollector.ts`
- `src/srs/incremental-reading/*`
- `src/hooks/useIRReadingBreakpoint.ts`
- `src/hooks/useIRShortcuts.ts`
- `src/hooks/useIRSessionTimer.ts`
- `src/components/incremental-reading/*`
- `src/components/incremental-reading/workspace/*`
- `src/styles/ir-workspace.css`
- `src/components/IncrementalReadingSessionRenderer.tsx`（薄兼容层）
- `src/components/IncrementalReadingManagerPanel.tsx`（薄兼容层）
- `模块文档/渐进阅读_低压体验优化计划.md`
- `模块文档/渐进阅读_优化路线.md`

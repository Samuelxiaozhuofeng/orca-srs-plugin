# 云端刷卡同步方案 · v0 定稿

> 状态：**规格已定稿 / 未实施**
> 目标：电脑端把卡片推到自建 VPS → 手机浏览器刷卡 → 电脑打开 Orca 后自动把结果落回本地块属性
> 产出方式：Claude（方案作者）× gpt-5.6-sol（独立评审）三轮对抗式评审收敛
> 关键结论均有代码行号或实测数据支撑，无推测

---

## 0. 结论

**可行，且技术地基比预想的牢。** 三轮评审后，架构主张收敛为一句话：

> **云端只记「谁在什么时候按了哪个键」，绝不计算记忆算法；电脑端是唯一账本，按严格顺序重放。**

这个主张有两条实测支撑（附录 A）：

- **重放确定性成立** —— 同样的输入和评分时刻，连算两次结果完全一致（FSRS 的抖动是按评分时刻确定性播种的）。
- **顺序是硬性要求，不是优化** —— 同样两次评分，`again→good` 与 `good→again` 算出的记忆稳定度是 **1.530 vs 2.090**，到期日差两天。

评审过程中被推翻或大幅修正的原始设计有 **6 处**，其中 **3 处是会导致数据损坏的阻断级问题**（§9 完整记录）。真正的工作量大头仍然是**把卡片搬到网页上还长得对**，不是同步协议本身。

---

## 1. 架构铁律

```
┌──────────────────────────┐                      ┌──────────────────┐
│  Orca 桌面（唯一账本）      │  ① push 冻结复习包 →  │   VPS（Fastify）  │
│                          │                      │                  │
│  块属性 srs.*            │  ← ② pull 评分事件    │  只存快照 + 事件   │
│  reviewLogs_YYYY_MM      │                      │  不算 FSRS        │
│  ③ 按序重放 + 原子标记    │  ④ ack →             │                  │
└──────────────────────────┘                      └──────────────────┘
                                                          ↑ 评分事件
                                                     手机 PWA
                                              （跑同版本 FSRS，仅管
                                                会话内何时重现，非权威）
```

**四条不可让步：**

1. **服务端永不计算权威 FSRS 状态。** 参数漂移 + `ts-fsrs` 版本漂移 = 静默分叉，用户几乎不可能发现。
2. **手机端可以跑 FSRS，但结果不具权威性**（见 §5，这是评审过程中被我的实测数据推翻后升级为 v0 必需项的设计）。
3. **不做双向状态合并。** FSRS 不可交换，两边各算完再 merge 本质无解。
4. **过去的到期时间原样保留，绝不平移。** 平移 = 在没有新评分的前提下篡改调度历史。

---

## 2. 持久身份（原设计的第 1 个阻断级修正）

### 为什么不能用 blockId

原设计打算直接用现有的 `cardKey`（`src/srs/cardIdentity.ts` 的 `buildCardKey()`）当云端主键。**这是错的。**

`DbId = number`（`src/orca.d.ts:4477`）是本地数据库行号。文档从未承诺它跨「删除重建 / 复制 / 导入导出 / Orca 自身跨设备同步」保持不变。剪切粘贴到底是 move 还是「删除+新建」，取决于 Orca 实现，不能假设。云端一旦用它当永久主键，事件回来时可能静默对不上卡。

### 定稿方案

| 卡型 | 云端永久主键 |
|---|---|
| 普通卡 | `{blockUuid}` |
| 填空卡 | `{blockUuid}:c1` |
| 正反向卡 | `{blockUuid}:forward` / `:backward` |
| 列表条目 | `item:{itemUuid}` —— **不含父 UUID** |

- 首次导出时给块写 `srs.uuid`（String，type=2，规范化 UUID）
- **列表条目主键不含父 UUID**：否则把条目移到另一个父块下会改变主键，违背"永久身份"的目的。父 UUID 只作关系字段保存
- 现有 `cardKey` 降级为**本地定位信息**，不再是云端身份
- 检测到 UUID 重复（复制块导致）、身份缺失、同一 key 对应两张活卡 → **停止同步并显示冲突**，不静默合并

### 变体编号漂移

实测结论（`src/srs/storage.ts:766` 注释 + `src/srs/imageOcclusion.ts:852`）：

- **文字填空卡不会自动重编号** —— 代码明确保证"已有 `srs.cN.*` 时绝不覆盖"
- **图片遮罩卡会** —— 删除一个遮罩后，后续编号会被 `moveClozeCardSrsData` 往前压（c3→c2）

因此：**v0 明确规定「改填空编号 = 删除旧卡 + 新建卡」，不自动搬运旧进度**；需要保进度的重编号迁移工具放 v1。图片遮罩卡因为编号会漂移，必须每个遮罩单独发 UUID —— 它本来就排在最后一期，不影响 v0。

---

## 3. 本地重放的幂等（原设计的第 2 个阻断级修正）

### 原设计的数据损坏路径

原设计是「写状态 → flush 日志 → ack」。若**状态写成功、日志写失败或进程崩溃**，事件不会 ack，下次重试会把同一次评分**再应用一遍 FSRS**。复习日志按 `event_id` 幂等**保护不了块属性里的状态**。

### 定稿方案：块内原子应用标记（不需要额外 journal）

关键事实：本地写属性走 `orca.commands.invokeEditorCommand("core.editor.setProperties", null, [blockId], properties)` —— `properties` 是**一个数组、一次调用、单个编辑器事务**（`src/srs/storage.ts:451/651/1102` 均为此形状）。

所以在写 FSRS 状态的**同一个属性数组里**追加三个字段，原子同写：

| 属性 | 说明 |
|---|---|
| `srs.{变体前缀}lastAppliedEvent` | 刚应用的 `event_id` |
| `srs.{变体前缀}syncRevision` | 逻辑卡版本号，每次 +1 |
| `srs.{变体前缀}stateHash` | 应用后的状态摘要 |

> ⚠️ **标记必须带变体前缀**（`srs.c1.lastAppliedEvent` / `srs.forward.lastAppliedEvent`）。
> 用块级共享标记会让同一块的 c1/c2、正/反向互相覆盖 —— 这是评审抓到的、我原提案里的真漏洞。

由此得到：
- 状态与"已应用哪个事件"不可能一个成功一个失败
- 重放前先读标记：已含此 `event_id` → **跳过状态应用**，只补日志和 ack
- 日志写失败变成良性问题（按 id 幂等，可重试），不再污染 FSRS 状态
- 常量大小，不需要存最近 N 个 id，也不需要 journal 文件

**必须同时满足的约束：**

1. 同一逻辑卡**串行处理** —— 事件 N 的「状态 + 日志 + ack」未完成前，不得应用 N+1
2. 事件必须携带完整 `pre_state` —— 否则状态写完后崩溃，将无法重建日志内容
3. 服务端**不得删除未 ack 事件** —— 否则本地只剩标记、没有补日志的材料

---

## 4. 事件排序（原设计的第 3 个阻断级修正）

原设计只按 `reviewed_at` 排序。**不够**：手机时钟可能不准；离线补交时事件可能按网络到达顺序乱序上传，导致先到的后一个事件被应用、后到的前一个事件被误判为过期丢弃。

**定稿：每个手机会话有连续递增的 `session_seq`**（在手机 IndexedDB 中与事件原子递增），服务端加唯一约束 `(session_id, session_seq)`。

- 事件按 `session_seq` 定序，**手机时间不参与定序**
- 服务端只交付**连续前缀**或已结束的完整会话
- 本地发现序号缺口 → 停在缺口处报错，不跳过
- 会话开始时取服务器时间锚点；发现明显时钟跳变或同卡时间倒退 → 隔离，不人工捏造时间

---

## 5. 手机端跑 FSRS（被实测数据推翻的设计）

原设计打算手机端不跑算法，`again` 一律放队尾。**实测推翻了这个简化。**

附录 A 的场景 1 显示：新卡在手机上评一次 good，进入"学习中"状态、到期时间是 **10 分钟后**。若会话就此结束，晚上重放时这个到期时间已成过去 → 卡片立刻又到期。而场景 2 显示，只要在会话内把学习步走完（again→good），到期时间就落在次日，完全正常。场景 3 显示成熟卡（到期 37 天后）根本没这个问题。

**结论：「重放后立刻又到期」只发生在会话结束时卡还停在学习步中间。** 手机端能不能正确决定"这张卡 10 分钟后再考一次"，直接决定这个问题的发生频率。

**定稿：手机端打包同版本 `ts-fsrs`，用冻结配置和冻结 pre-state 维护会话内临时状态。**

- 只决定**会话内何时重现**，不上传计算出的状态
- 电脑端仍用相同输入独立重算，作为唯一账本
- 附带收益：手机能算出 `expected_post_state_hash`，供电脑端做一致性校验（§7）
- 成本比看起来低 —— 手机本来就要持有 pre-state 和冻结配置，剩下就是同一个 `fsrs.next()` 调用

**仍不能保证用户走完学习步。** 用户提前结束会话时，过去的到期时间原样保留、可能立即重新到期；手机界面应显示"还有学习中的卡片"，**而不是偷偷平移到期时间**。

---

## 6. FSRS 配置冻结

**问题**：`getFsrsInstance(pluginName)`（`src/srs/algorithm.ts:229-240`）在**每次调用时**从 `orca.state.plugins[name].settings` 重读权重并改写模块级单例 `fsrs`。即重放用的是**重放时刻的参数**，不是评分时刻的参数。参数变更、插件升级、`ts-fsrs` 版本变化都会改变结果。

**定稿：**

- **新增** `nextReviewStateWithConfig(prevState, grade, now, validatedConfig)` —— 纯函数、自建实例、**不碰现有单例**。
  （为同步功能去重构 `getFsrsInstance` 的爆炸半径覆盖全部复习路径，风险远大于收益。）
- 服务端存**不可变** `scheduler_config`：21 权重 + requestRetention + maximumInterval + 短期学习/fuzz 等所有影响状态转换的开关 + `ts-fsrs` 版本 + 算法修订号
- 事件携带 `scheduler_config_id`
- **处理规则（不向用户提问）**：
  - 当前设置与事件不同，但旧配置和算法仍可运行 → **自动按冻结的旧配置重放**
  - 配置缺失/损坏，或当前插件跑不了那个旧算法版本 → **隔离**，提示升级到兼容版本或明确放弃该事件
  - **绝不询问用户"按旧参数还是新参数"** —— 按新参数会破坏确定性，而且这是普通用户答不上来的问题

---

## 7. 一致性校验：两级，不是单一硬哈希

评审规格原本要求「结果哈希 ≠ 预期哈希 → 视为程序错误停止」。**我认为这条会变成误报地雷，做了修正。**

依据（`node_modules/ts-fsrs/dist/index.mjs`）：
- 记忆强度计算大量使用 `Math.exp` / `Math.pow`（line 830/847/859/918），而 ECMAScript 规范对这两个函数**只要求实现近似**，不保证跨引擎逐位一致
- 手机是 JavaScriptCore(iOS Safari) / Android V8，电脑是 Electron V8 —— **不是同一引擎**
- 但 ts-fsrs 每一步都做 `.toFixed(8)`（line 785/813/833/850/861/919），且 `scheduled_days` 走 `Math.round`（line 759/776），**到期日是从整数天数派生的**

### 定稿规则（我的初版提案被评审驳回了一半，这是修正后的）

> 我最初提议「记忆强度超容差就记 warning、继续应用、本地为准」。**这条被否掉了，理由成立**：
> 错误的权重或错误的前态**完全可能算出相同的整数间隔**、只在记忆强度上有差异。
> 所以"逻辑 bug 必然体现在整数字段上"不成立，软放行是不安全的。

**(1) 哈希前先规范化** —— 记忆强度 / 难度先取整到 6 位小数再入哈希。

> 这一步是关键，也是我原提案漏掉的连锁反应：
> **如果容忍浮点差异、却对原始浮点做哈希，分歧只会转移到下一个事件的前置校验上**
> —— 下一条事件的 `pre_state_hash` 照样对不上。规范化必须两端用同一套规则。

**(2) 哈希内容** —— `state` + `due`（截到秒）+ `scheduled_days` + `reps` + `lapses` + **规范化后的**记忆强度 / 难度。

**(3) 原始浮点用混合容差另行比对**：

```
abs(a - b) <= 1e-6 + 1e-9 * max(|a|, |b|)
```

（纯相对误差在接近零时不适用，所以用混合式。）

| 结果 | 处理 |
|---|---|
| 容差内 | 视为跨引擎等价，正常继续 |
| **超容差** | **第一次就隔离该逻辑卡**，保留可重试状态；**不累计计数、不继续应用** |

超容差更可能意味着配置、顺序或实现分歧，而不是浮点噪声。计数阈值既会放过真实错误，也解决不了副本持续分叉的问题。

> 如果将来产品上必须保证"同步永不中断"，正确做法是**让事件携带权威 post-state 数值**、接收端校验后直接采用，而不是各端各自保留自己的浮点结果 —— 只有哈希是无法收敛的。这属于 v2 范畴。

---

## 8. 事件字段表

| 字段 | 含义 |
|---|---|
| `schema_version` | 事件格式版本 |
| `event_id` | 全局 UUID，同时是复习日志的幂等键 |
| `device_id` | 配对设备 |
| `session_id` / `session_seq` | 会话 + 严格递增顺序（**定序依据**） |
| `queue_pack_id` | 来源冻结包 |
| `card_key` | 不透明持久卡片身份（§2） |
| `content_hash` | 用户评分时看到的内容版本 |
| `event_type` | v0 固定为 `review` |
| `rating` | again / hard / good / easy |
| `reviewed_at` | 经服务器时间锚点校正的 UTC 时间 |
| `client_wall_at` / `timezone_offset` | 诊断与日期归属 |
| `duration_ms` / `duration_capped` | 有效时长（见 §11） |
| `scheduler_config_id` | 冻结算法配置 |
| `pre_revision` / `pre_state` / `pre_state_hash` | 评分前完整状态，可独立重建状态与日志 |
| `expected_post_state_hash` | 手机用冻结算法算出的预期结果（§7） |
| `previous_card_event_id` | 同卡前一个手机事件，可为空 |

---

## 9. 电脑端重放算法

```text
拉取最早未结算会话，按 session_seq 排序
若序号有缺口，或同一序号出现不同事件：停在缺口并报错

逐事件处理：
  校验身份、冻结描述符、content_hash、同卡事件链
  读取该逻辑卡的 状态 / syncRevision / stateHash / lastAppliedEvent

  若 lastAppliedEvent == event_id：
      幂等补写 ReviewLog → ack → continue        # 崩溃恢复路径

  若 pre_revision 或 pre_state_hash 与当前不符：
      隔离该事件及其后续同卡事件；其他无依赖的卡继续 → continue

  加载 scheduler_config_id；配置/算法不可用 → 隔离
  用 nextReviewStateWithConfig(pre_state, rating, reviewed_at, config) 计算

  硬校验哈希不符 → 停止（软校验不符 → warning，继续）

  单次属性事务写入：
      FSRS 新状态 + syncRevision+1 + stateHash + lastAppliedEvent
  invalidateBlockCache
  以 event_id 幂等写 ReviewLog（saveAndFlushReviewLog）
  成功后逐事件 ack
```

**属性写、日志写、ack 任一失败都保持错误可见并可重试**（对齐仓库 Non-negotiable：禁止空 catch、禁止静默返回空值）。
**过去的到期时间原样保存，不平移。**

### 冲突分支

| 情况 | 处理 |
|---|---|
| 当前配置不同但旧配置可运行 | 自动用冻结旧配置，**不算冲突** |
| 电脑端进度已变化 | 默认保留电脑端状态；手机事件**不写复习日志**（避免污染统计），挂起等用户处理 |
| 卡片内容已变化 | 询问是否仍承认这次评分，默认不自动应用 |
| 卡片已删除 / UUID 冲突 | 隔离，不自动恢复或改绑 |
| 旧算法不受支持 | 要求升级兼容版本，或明确放弃该事件 |
| 硬校验哈希不符 | 视为实现错误，**禁止用户随意"继续应用"** |

---

## 10. 冻结复习包与队列

**服务端不做任何队列逻辑。** 插件在推送时按现有规则（冻结作用域、`newCardsPerDay` / `reviewCardsPerDay` 上限、暂停/待激活过滤）算好包再上传，服务端原样吐给手机。

理由：队列规则是本仓库最复杂、回归最密的部分之一（`cardCollector.queueLimits.test.ts` 等一整组测试）。在服务端重写一遍 = 保证两边行为漂移。

**改进（评审建议，优于原设计的单一冻结队列）：上传多个顺序固定的 queue pack** —— 正常今日队列 + 预备追加包 + 明确标记的额外学习包。手机上的"再来 20 张"只是**释放下一个预存包**，不在服务端重新实现筛选规则。

> 产品限制需明确告知用户：电脑长时间不开时，能追加多少完全取决于之前预存了多少。

冻结描述符包含：作用域、每日上限、主队列、追加包、卡片内容与 `content_hash`、每卡 FSRS pre-state、revision / stateHash、不可变 `scheduler_config_id`、服务器时间锚点。

**v0 只允许一个未结算的手机会话**，新会话必须等电脑端回放并上传新快照。

---

## 11. 卡面渲染（✅ spike 四轮已闭环，风险清零）

> **2026-08-11 两轮实测结论（详见 §19）：**
>
> - ✅ **`blockConvert("html", ...)` 可用** —— 真实卡片烘焙出正常 `<p>` 结构，中英文与标点完好。**路径 1 成立。**
> - ✅ **内容面远比预想窄** —— 抽样中**无图片、无代码块、无公式、无表格**。实际只有三种成分：**纯文字 + 块引用 + 填空**。这是本方案最大的利好，原先担心的大量保真问题不存在。
> - ✅ **缺陷 A（填空标记被抹平）已验证修复** —— 运行时补注册 `html` 行内转换器后：
>   `<p>limericks：指…</p>` → `<p><span data-cloze="1">limericks</span>：指…</p>`
>   且填空片段**自带 `clozeNumber` 字段**（完整结构为 `{t, v, clozeNumber}`），多填空卡可区分。
>   落地位置：`src/srs/registry/converters.ts` 补 `registerInline("html", "orca-srs.cloze", …)`。
> - ✅ **缺陷 B（块引用退化成编号）已定位并验证解法**
>   - **真实根因（实测，非推断）**：引用**有无 `alias`** 决定成败，与「目标块是否加载」无关。
>     - `#17904`：`{id:307, to:18352, alias:"真相"}` → `<span>真相</span>` ✅
>     - `#18084`：`{id:281, to:18085, alias:null}` → `<span>281</span>` ❌ —— alias 为空即回退成编号
>   - **映射关系**：`content` 片段的 `v` 是**引用记录 id**，**不是目标块 id**；
>     `片段.v` → `block.refs` 中同 `id` 的记录 → `alias`，为空则取 `to` 指向的块文本。
>     （v3 的修复失败正是因为把引用记录 id 当块 id 去预取，抓回了不相干的块。）
>   - **宿主路线全灭**：`getRefById` / `getBlockById(正确目标块)` / 两者同时给 —— **三种都无效**，
>     转换器不消费这两个回调。⚠️ 因此 `ConvertContext` 的这两个字段**不可依赖**。
>     （另注：本地 `src/orca.d.ts:4593` 的 `ConvertContext` 只有 `exportRootId`，已过期；
>     权威定义见 `plugin-docs/types/plugin-runtime-types.md:2311`。）
>   - ✅ **采用方案：插件自行解析引用**（实测已跑通，`281` → `摩尔定律`）。
>     比依赖宿主更可控 —— 映射完全在我们手里，不会因宿主升级而漂移。


复习界面用的是 `orca.components.Block`（宿主 React 组件，`SafeBlockPreview.tsx` / `CardBlockPreview.tsx`），**搬不到网页**。而 `ReviewCard.front/back` 只是纯文本（`src/srs/blockUtils.ts:60`），丢格式、丢引用、丢图片、丢代码块。

**`orca.converters.blockConvert` 只能当候选实现，不能当架构支点** —— 文档仅说明它是导出转换器，没有承诺完整 CSS、引用解析、资源 URL 改写、插件自定义块或 HTML 安全（`plugin-docs/types/plugin-runtime-types.md:169`）。

**一个有利事实**：插件目前只注册了 `plain` 格式的转换器（`src/srs/registry/converters.ts`），**没有注册 `html`**。这意味着填空片段怎么序列化成 HTML 标记，我们有**完全的控制权** —— 这正是烘焙 `<span data-cloze="N">` 的天然位置。

### 定稿的烘焙管线（spike 四轮后确定）

```
块 content 片段
  ↓ ① 预处理（插件自己做，不依赖宿主）
      · r 片段 → 解析成文字片段：refs 中同 id 记录的 alias
                 ；alias 为空则取 to 指向块的文本
                 ；取首行、trim、截断 ~50 字（实测目标块文本带尾换行，必须 trim）
  ↓ ② orca.converters.blockConvert("html", 预处理后的 content, ...)
      · 填空片段由我们注册的 html 行内转换器输出 <span data-cloze="N">
  ↓ ③ HTML 清洗（白名单标签/属性）
  ↓ 上传
```

> **为什么在①做片段级预处理，而不是对产出的 HTML 做字符串替换：**
> `blockConvert` 的 `blockContent` 参数由我们自己构造，**输入完全可控**，替换结果确定；
> 而正则替换 `<span>281</span>` 依赖输出格式、且理论上可能误伤正文内容。
> spike 里用的是后者（快速验证），**落地时用前者**。

**三条路径，按优先级：**

1. 对验证通过的核心内容用 `blockConvert`，随后**严格清洗 HTML + 重写资源地址**
2. 长期建立受支持子集的 **portable AST 渲染器**，直接消费 `content` fragments —— 最可控
3. 无法保真的块用 **PNG 快照或纯文本显式降级**，并在同步前显示警告，**不静默丢内容**

**动工前必须先做真实 spike**，覆盖：引用、图片、代码、公式、换行、插件自定义 inline、恶意 HTML。

### 图片资源

图片存在本地仓库 `{repoDir}/assets/`（`src/srs/repoAssetPath.ts`），网页拿不到 `file://`。必须按内容哈希去重上传；先 `HEAD` 探测再传缺的。

---

## 12. 本地代码改动清单

| 改动 | 位置 | 规模 | 风险 |
|---|---|---|---|
| `parseCardKey()` + 与 build 的 round-trip 属性测试（`fast-check` 已在依赖里） | `src/srs/cardIdentity.ts` | 小 | 低 |
| `srs.uuid` 持久身份写入 + 重复检测 | 新增 `src/srs/sync/identity.ts` | 中 | 中 |
| `nextReviewStateWithConfig()` 纯函数入口（**不动现有单例**） | `src/srs/algorithm.ts` | 小 | 低 |
| 按指定时刻评分的 `*At` 变体（普通 / cloze / direction） | `src/srs/storage.ts` | 小 | 低 |
| 应用标记原子写（`lastAppliedEvent` / `syncRevision` / `stateHash`，带变体前缀） | `src/srs/storage.ts` | 中 | **高（正确性）** |
| **注册 `html` 行内转换器**（填空 → `<span data-cloze="N">`，用片段自带的 `clozeNumber`） | `src/srs/registry/converters.ts` | 小 | 低（**已实测验证**） |
| **引用解析预处理**（`r` 片段 → 文字：`refs[].alias` ?? 目标块首行，trim + 截断） | 新增 `src/srs/sync/refResolver.ts` | 小 | 低（**已实测验证**） |
| 卡面烘焙器（预处理 → `blockConvert` → 清洗 + 资源收集） | 新增 `src/srs/sync/snapshotBuilder.ts` | 中 | 中（**风险已从「高」降级**） |
| 同步客户端（push / pull / ack / pending 重试） | 新增 `src/srs/sync/` | 中 | 中 |
| 重放器（三态存在性、冲突分支、两级校验、幂等日志） | 新增 `src/srs/sync/replay.ts` | 中 | **高（正确性）** |
| 定时器 + 窗口聚焦触发 + 广播刷新 | `src/main.ts`、`srsBroadcastBus.ts` | 小 | 低 |
| unload 时 flush 未 ack 队列 | `pluginUnloadSequence.ts` | 小 | 低 |
| 设置页 + 扫码配对 UI + 冲突处理界面 | `settings/` + 新组件 | 中 | 低 |
| 模块文档 + `模块文档/README.md` 条目 | 新增 `模块文档/云端同步.md` | 小 | — |

**必须遵守的既有约束**：批量读块沿用 `BLOCK_PREFETCH_BATCH_SIZE=50` / `BLOCK_PREFETCH_CONCURRENCY=4`，禁止无上限 `Promise.all`；块存在性用三态，`unknown` **不得**当 `missing` 处理（不删除、不 ack、留待下次）；错误一律可见。

---

## 13. 服务端选型与部署

**Node.js LTS + TypeScript + Fastify + SQLite(WAL) + React/Vite PWA + Caddy + Docker Compose**

- 前后端与插件共用 TypeScript schema
- SQLite WAL 足够覆盖单用户单实例，**不需要** PostgreSQL / Redis / 消息队列
- 表：`repo`、`device`、`scheduler_config`、`card_snapshot`、`queue_pack`、`review_session`、`review_event`、`asset`
  （`repo` 与 `device` 必须拆开 —— 原设计把 `device_label` 塞进 repo，无法表达"同一仓库两台电脑"）
- **备份必须用 SQLite 在线备份机制并同时备份资源目录**；WAL 开启时不能只复制主库文件，WAL 本身是持久状态的一部分
- 只保留两个容器：`app` + `caddy`（自动申请续期 HTTPS）；Compose 固定镜像版本、持久卷、healthcheck、`restart: unless-stopped`
- v0 **不上** SSE / Kubernetes / 对象存储 / 多节点，60 秒轮询足够

### 鉴权（不做用户名密码）

- 首次部署生成一次性管理员恢复密钥
- 电脑通过一次性 setup token 配对，获得独立 `desktop-sync` token
- 手机扫描**短时、单次**二维码，换取独立且可撤销的 `phone-review` 凭据
- 服务端只保存高熵 token 的**哈希**；每个设备可单独撤销
- 网页端换成 `Secure` + `HttpOnly` + `SameSite=Strict` cookie，**不把长期 token 放 localStorage**
- 限制请求体与资源大小，启用严格 CSP，所有烘焙出的 HTML **显示前必须清洗**
- 复用现有 `src/srs/http/redactSecrets.ts` 做日志脱敏

### 加密取舍

v0 **不做**端到端加密，仅限单用户私有服务器 + HTTPS + 受保护的磁盘和备份。
生产版建议：正文与资源用 AES-GCM 加密（独立内容密钥 + 随机 nonce + `key_version` + 恢复密钥），调度元数据明文。
**但要如实告知**：网页代码本身也由 VPS 下发，所以加密主要防数据库/备份泄漏，**不能完全防一台已被攻陷的服务器向浏览器投放恶意脚本**。

---

## 14. 数据保留与清理

| 对象 | 保留 |
|---|---|
| 已 ack 事件 | 90 天 |
| 未 ack / 冲突中事件 | **永不自动删除**，直到用户处理 |
| 已删除卡 | 写 tombstone；快照与 tombstone 保留 90 天，且**无未处理事件**才可清理 |
| 孤儿资源 | 最后失去引用 30 天后删除；仍被快照 / 冻结会话 / 未处理事件引用时不得删除 |

提供「立即彻底删除云端数据」的显式管理操作。

---

## 15. 复习时长口径

本地既有规则（`src/srs/sessionProgressTracker.ts:123`）：墙钟时间截断到 `0..60000ms`，且**电脑端不因窗口隐藏/失焦而暂停**。

手机端定稿：从卡片完整显示到点击评分，用**单调时钟**累计，**页面隐藏 / 失焦 / 锁屏 / 切换应用期间暂停**，单次封顶 60 秒并记 `duration_capped=true`。跳过不产生复习日志。

> 页面亮着但用户离开时，程序无法区分"认真思考"和"去吃饭"；60 秒封顶是诚实且可控的边界，不假装拿到了更精确的数据。

---

## 16. v0 范围

**做**：普通卡、显式编号填空卡、正/反向卡、独立列表条目；文本 + 图片的只读复习；四档评分 + 本次跳过。

**不做**：组合型特殊卡、图片遮罩、渐进阅读 / Book IR、手机端编辑、持久暂停 / 推迟 / 重置、音频附件、多设备并发。

> **渐进阅读明确排除**：它需要回写摘录内容，是双向写，直接破坏 §1 的单向真相铁律。
> **持久暂停排除的理由**：suspend / postpone / reset 都是新的持久状态命令，各自需要独立的冲突、日志和撤销语义。v0 用"本次跳过"（只影响当前会话、不写调度状态）已足够应付烂卡。

---

## 17. 分期

- **v0** —— 单用户自托管、扫码配对、冻结复习包、手机离线评分、手机端临时 FSRS、原子应用标记、幂等日志、冲突队列、基础统计与清理。**目标是正确闭环，不追求覆盖桌面全部能力。**
- **v1** —— 持久暂停 / 推迟 / 重置的独立语义；音频与更多内容类型；填空重编号迁移；旧算法恢复工具；更友好的冲突处理；后台同步与会话提醒。
- **v2** —— 多手机 / 多电脑并发、跨会话因果排序、自动合并策略、可选端到端加密或多用户部署。

**Anki 替代路线的结论**：只有当用户接受**「Anki 成为真正的调度账本」**时才更简单。若仍要求 Orca 内的 FSRS 状态是唯一权威，Anki 只是多了一层身份映射 + 日志提取 + 调度差异，**并没有省掉最难的回写问题**。不建议"把 Anki 当临时手机 UI 再无损回灌 Orca"。

---

## 18. 用户已拍板的决定

> **状态：2026-08-11 已确认，全部采用推荐项。**
>
> | 决定 | 结果 |
> |---|---|
> | 使用流程 | ✅ 接受「电脑备包 → 手机刷 → 回电脑结算」，同时只允许一批未结算 |
> | 云端隐私 | ✅ 私有服务器 + HTTPS，**v0 不做端到端加密** |
> | 卡面还原不了时 | ✅ **先做 spike 实验拿真实结果再定**（见 §19） |
> | 首版范围 | ✅ 接受 §16 范围（不含渐进阅读 / 图片遮罩 / 手机端暂停推迟） |
>
> 下表其余各条均按推荐项执行，无待决项。

| # | 问题 | 推荐 | 不选推荐项的代价 |
|---|---|---|---|
| 1 | 手机上的复习节奏要不要和电脑完全一致？ | **要** | 手机上"再来一次"的时机会偏早或偏晚，且更容易出现"晚上回电脑发现卡又冒出来" |
| 2 | 能接受「电脑先备好一批卡 → 手机刷完 → 回电脑结算」这个流程吗？ | **接受**，且同一时间只允许一批未结算 | 不接受就得做多设备并发合并，复杂度翻几倍 |
| 3 | 首版支持哪些卡？ | 普通卡、填空卡、正反向卡、列表条目 | — |
| 4 | 首版支持哪些附件？ | 文字 + 图片；音频等以后再说 | — |
| 5 | 手机上遇到烂卡能做什么？ | **只能"本次跳过"**；永久暂停回电脑做 | 暂停/推迟都是持久状态，各需一套冲突和撤销逻辑 |
| 6 | 电脑和手机对同一张卡都动过时保留哪边？ | **保留电脑**，被放弃的手机评分不计入统计 | — |
| 7 | 手机刷卡期间，那张卡在电脑上被改了内容怎么办？ | 暂停该卡同步、问你一次，不自动承认 | — |
| 8 | 改填空编号算不算新卡？ | **算**，v0 不自动搬运旧进度 | 自动搬运需要一套迁移工具，放 v1 |
| 9 | 允许两部手机同时刷吗？ | **不允许**，后登录的只能看状态 | — |
| 10 | 云端能不能存卡片的文字和图片？ | 可以，但限定**你自己的私有服务器 + HTTPS**，v0 不加密 | 要加密就得接受"恢复密钥丢了数据就没了" |
| 11 | 服务器上的历史留多久？ | 已确认记录和已删卡 90 天，孤儿图片 30 天，**没处理的冲突永不自动删** | — |
| 12 | 在一张卡上停很久怎么计时？ | 页面切走不计时，单张最多记 60 秒并标记已封顶 | — |

---

## 19. 卡面还原 spike —— ✅ 已完成（2026-08-11，四轮）

> **结论：卡面渲染这块风险已清零，可以开工。**
>
> | 轮次 | 结论 |
> |---|---|
> | v1 | ✅ `blockConvert("html")` 可用，文字完好；但样本被渐进阅读卡污染 |
> | v2 | ✅ 内容面很窄：**只有 纯文字 + 块引用 + 填空**，无图片/代码/公式/表格 |
> | v3 | ✅ 填空修复验证通过；片段自带 `clozeNumber`。引用修复失败（实现错误） |
> | v4 | ✅ 引用根因查明（`alias` 为空即回退编号）；宿主三种解法全灭；**插件自行解析跑通** |
>
> **两个缺陷都已有验证过的解法，均为小改动**（见 §12 前两行）。
> **待补验证的次要项**：多填空卡（全库无样本）、引用目标块文本过长时的截断表现。

---

## 附：spike 过程与脚本

用户已拍板「先做实验再定降级策略」。**这是动工前必须先跑的一步**，因为它是整个方案唯一不确定的部分，且结论会反向影响 §11 的路径选择和 §16 的卡型范围。

### 怎么做

**不需要改代码、不需要编译。** 脚本已备好：[`云端刷卡_卡面烘焙spike.js`](云端刷卡_卡面烘焙spike.js)

在 Orca 里打开开发者工具（macOS `Cmd+Option+I`）→ Console → 整段粘贴回车。**只读，不修改任何笔记数据。**

脚本做五件事：
0. **体检转换器注册表** —— 先看宿主到底有没有 `html` 这条路（若没有，直接走路径 2，省掉后面全部工作）
1. 按 `#card` 标签抓卡片
2. 统计**你的卡片里实际出现哪些内容类型**（不是理论清单，是真实分布）
3. 逐张烘焙正面/背面，标记 ✅ / ⚠️空 / ❌报错
4. 重点项自动检查：图片 src 长什么样、块引用变成什么、有没有需要清洗的脚本属性
5. 弹出一个**不带 Orca 样式**的预览窗口 —— 那就是手机浏览器里的样子

### spike 要回答的问题

> 用 `orca.converters.blockConvert("html", ...)` 把真实卡片烘焙成 HTML，**在普通浏览器里打开，看还剩多少**。

### 必须覆盖的内容类型

| 类型 | 关注点 |
|---|---|
| 纯文字 + 基础格式 | 加粗 / 斜体 / 高亮是否保留 |
| **块引用 / 双链** | 引用目标是否解析成文字，还是留下坏链 |
| **图片** | 路径是否被改写成可替换的形式（这决定资源上传方案） |
| 代码块 | 换行与缩进是否保留 |
| **公式** | 是否需要额外前端库 |
| 表格 / 列表 | 结构是否保留 |
| **填空片段** | 插件自定义 inline，**当前只注册了 `plain` 转换器**，需确认 html 缺省行为 |
| 长文本换行 | 段落结构 |
| **恶意 HTML** | 清洗管线是否必要（结论：必然必要，此项只为确定清洗强度） |

### 第一轮结果（2026-08-11）

| 项 | 结果 |
|---|---|
| HTML 转换可用性 | ✅ **可用** —— 每张卡都产出正常 `<p>`，文字/标点/中文完好 |
| 填空标记 | ❌ **被压平** —— `orca-srs.cloze` 片段烘焙后标记消失，只剩完整句子 |
| 图片 / 代码 / 公式 / 块引用 | ⚠️ **零命中，未验证** —— 样本被渐进阅读纯文字章节占满 |
| 脚本 / 危险属性 | 本轮未出现（样本太干净，不足以下结论） |

**两个操作结论：**

1. **填空标记压平不是路线问题，是缺注册。** `src/srs/registry/converters.ts` 目前只注册了 `plain` 格式的块与行内转换器（含 `${pluginName}.cloze`），**没有 `html`**。补注册即可，且正好是我们控制填空标记形态的天然位置。这条反而**印证了路径 1 可行**。
2. **首轮样本不可用于决策。** 需 spike v2 定向复测。

### 第二轮结果（2026-08-11，已排除渐进阅读卡）

| 项 | 结果 |
|---|---|
| 图片 / 代码 / 公式 / 表格 | ✅ **抽样中完全没有** —— 内容面窄，风险大幅下降 |
| 纯文字 | ✅ 完好 |
| 块引用 | ❌ **间歇性退化成块编号**（取决于目标块是否在内存） |
| 填空标记 | ❌ 确认被抹平（答案文字仍在 `fragment.v`） |

**结论：v0 只需解决两个缺陷，且都不需要更换技术路线。**

### 第三轮（v3）：把两个修复从「推断可行」变成「已验证」

第三版脚本不再普查，而是**当场证伪两个修复方案**：

- **修复 A** —— 运行时临时注册 `html` 填空转换器，看能否输出 `<span data-cloze="N">`（结束时立即注销，`finally` 保证）
- **修复 B** —— 预取被引用块并通过 `getBlockById` / `getRefById` 注入，看引用能否稳定显示文字
- 附带：不截断地打印填空片段完整结构，确认**是否携带填空编号**（决定多填空卡能否区分）；对照 `r` 片段的 `v` 与 `block.refs`，判定 `v` 是 refId 还是目标块 id

> 该脚本只在内存中临时注册转换器并立即注销，不写笔记数据、不改插件文件。

### 第二轮（v2）针对性改动

- 按 `ir.*` 属性**排除渐进阅读卡**
- 全库内容普查 → 按「内容丰富度」排序抽样，**专找有图片 / 非纯文字片段 / 真实块引用的卡**
- 填空卡按真实结构处理（答案在同句内，不在子块 —— v1 把它误报成"背面空"）
- 单独探测 `inlineConvert("html", "orca-srs.cloze", ...)`，隔离压平问题并确认修复位置

### 交付物

1. 一份对照表：每种类型「完美 / 可接受降级 / 不可用」
2. 若「不可用」项集中在少数类型 → 采用 §11 路径 1（`blockConvert` + 清洗）
3. 若「不可用」面积大 → 直接上 §11 路径 2（portable AST 渲染器），并相应收窄 v0 卡型范围
4. 据此最终确定降级策略：标注降级显示 / 拒绝上传 / 混合

**在 spike 出结论前，不要开始写同步客户端和服务端。** 服务端和协议部分风险已知且可控，卡面才是会推翻范围的那一块。

---

## 附录 A：重放行为实测

用仓库内 `ts-fsrs` 实跑（非推测）：

| 场景 | 结果 |
|---|---|
| 抖动决定性（同输入同时刻连跑两次） | **完全一致** —— 重放可复现成立 |
| 顺序敏感性 `again→good` vs `good→again` | stability **1.530 vs 2.090**，到期差 2 天 —— **严格排序是硬性要求** |
| 新卡 10:00 评 good | → 学习中，到期 10:10。22:00 重放时**已过期，立刻又到期** |
| 10:00 again → 10:05 good | → 走完学习步进复习态，到期次日 10:05，**不会立刻到期** |
| 成熟卡（复习态，间隔 10 天）评 good | 到期 37 天后，**完全无过期问题** |

---

## 附录 B：评审中被推翻或修正的原始设计

| # | 原设计 | 判定 | 修正 |
|---|---|---|---|
| 1 | 用 `cardKey`（基于 blockId）当云端主键 | **阻断** | 改写持久 `srs.uuid`（§2） |
| 2 | 写状态 → 写日志 → ack | **阻断** | 状态与应用标记原子同写（§3） |
| 3 | 只按 `reviewed_at` 排序 | **阻断** | 加 `session_seq`（§4） |
| 4 | 手机端不跑 FSRS，again 放队尾 | 重要 | 被实测推翻，手机端跑同版本 FSRS（§5） |
| 5 | 冲突时比较 `lastReviewed` | 重要 | 改用 revision / stateHash（§9） |
| 6 | 单一冻结队列 | 重要 | 改多个 queue pack（§10） |
| 7 | 配置不一致时询问用户 | — | 评审否决，改自动用冻结旧配置（§6） |
| 8 | 硬哈希不符即停止 | — | 作者反向提出两级校验，**评审驳回其中"软放行"部分**，最终为「规范化后哈希 + 超容差即隔离」（§7） |
| 9 | 列表条目主键含父 UUID | — | 评审指出移动条目会改主键，改为纯 `item:{itemUuid}`（§2） |
| 10 | 应用标记放块级 | — | 评审指出变体会互相覆盖，改带变体前缀（§3） |

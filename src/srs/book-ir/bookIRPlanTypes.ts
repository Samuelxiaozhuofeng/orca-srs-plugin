/**
 * Versioned Book IR reading-plan schema (re-export + property helpers).
 */

export type {
  BookIRChapterOutcome,
  BookIRMode,
  BookIRMutationResult,
  BookIRPlanV1,
  InitializeBookIRRequest,
  AdvanceSequentialBookRequest
} from "../../importers/epub/types"

export { IR_BOOK_PLAN_PROP } from "../../importers/epub/types"
